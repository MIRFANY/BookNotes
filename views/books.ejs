<!DOCTYPE html>
<html>
  <head>
    <title>Book Notes</title>
    <link rel="stylesheet" href="/styles.css" />
    <style>
      body {
        display: flex;
        flex-direction: column;
        min-height: 100vh;
        background: #f8f8f3;
      }
      .main-wrapper {
        flex: 1;
        display: flex;
        justify-content: center;
        align-items: flex-start;
        margin-top: 2rem;
      }
      .container {
        width: 100%;
        max-width: 700px;
        background: #fff;
        border-radius: 12px;
        box-shadow: 0 2px 16px rgba(128, 128, 0, 0.1);
        padding: 2rem 2.5rem;
        margin-bottom: 2rem;
      }
      .book-list {
        margin: 0;
        padding: 0;
      }
      .book-list li {
        opacity: 0;
        transform: translateY(20px);
        animation: fadeInUp 0.6s forwards;
        animation-delay: var(--delay);
        background: #f8f8f3;
        border-radius: 8px;
        margin-bottom: 1rem;
        box-shadow: 0 1px 4px rgba(128, 128, 0, 0.05);
        padding: 1rem;
        display: flex;
        align-items: center;
        gap: 1.5rem;
      }
      @keyframes fadeInUp {
        to {
          opacity: 1;
          transform: none;
        }
      }
      .book-list img {
        height: 90px;
        border-radius: 4px;
        box-shadow: 0 1px 4px rgba(128, 128, 0, 0.08);
        background: #f8f8f3;
      }
      .actions {
        margin-top: 0.5rem;
      }
      /* Olive accent for links */
      a,
      .btn {
        color: #808000;
      }
      a:hover,
      .btn:hover {
        color: #556b2f;
      }
      header {
        background-color: #556b2f;
        color: white;
        padding: 10px 0;
        text-align: center;
        font-size: 2rem;
      }
      .btn {
        display: inline-block;
        padding: 8px 16px;
        margin-right: 10px;
        border: none;
        border-radius: 4px;
        background-color: #556b2f;
        color: white;
        text-decoration: none;
        font-size: 1rem;
        transition: background-color 0.3s;
      }
      .btn:hover {
        background-color: #808000;
      }
    </style>
  </head>
  <body>
    <header>Book Notes</header>
    <div class="main-wrapper">
      <div class="container">
        <a
          class="btn"
          href="/books/new"
          style="margin-bottom: 1rem; display: inline-block"
          >Add New Book</a
        >
        <ul class="book-list">
          <% books.forEach(function(book, i) { %>
          <li style="--delay: <%= i * 0.1 %>s">
            <img
              src="<%= book.cover_url %>"
              alt="Cover"
              onerror="this.style.display='none'"
            />
            <div class="book-details">
              <div class="book-title"><%= book.title %></div>
              <div class="book-author">by <%= book.author %></div>
              <div class="book-meta">
                Rating: <%= book.rating %> | Read on: <%= book.date_read ?
                book.date_read.toISOString().slice(0,10) : '' %>
              </div>
              <div class="book-meta"><%= book.review %></div>
              <div class="actions">
                <a class="btn" href="/books/<%= book.id %>/edit">Edit</a>
                <form
                  action="/books/<%= book.id %>/delete"
                  method="POST"
                  style="display: inline"
                >
                  <button type="submit">Delete</button>
                </form>
              </div>
            </div>
          </li>
          <% }) %>
        </ul>
      </div>
    </div>
  </body>
</html>
